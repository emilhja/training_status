# Intervals.icu credentials
# Find your athlete ID in the URL when logged into Intervals.icu (e.g., iABC123)
INTERVALS_ID=your_intervals_id

# Get your API key from Intervals.icu → Settings → scroll to bottom
INTERVALS_API_KEY=your_api_key_here

# Smashrun token
# Get from https://api.smashrun.com/explorer → Connect → copy access_token from URL
SMASHRUN_TOKEN=your_token_here

# Strava integration (optional — leave blank to disable)
# 1. Create app at https://www.strava.com/settings/api
# 2. Visit: https://www.strava.com/oauth/authorize?client_id=YOUR_ID&response_type=code&redirect_uri=http://localhost&approval_prompt=force&scope=activity:read
# 3. Copy the `code` from the redirect URL, then exchange it:
#    POST https://www.strava.com/oauth/token  (client_id, client_secret, code, grant_type=authorization_code)
# 4. Copy the refresh_token from the response
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REFRESH_TOKEN=

# Scheduler — cron expression for automated daily fetch (default: 6am every day)
# Set to empty string to disable the scheduler.
FETCH_SCHEDULE=0 6 * * *
